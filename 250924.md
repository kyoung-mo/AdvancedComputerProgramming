### 포인터 예제 Ex_3_1
```c
#include <stdio.h>

void main() {
	int i = 10, j = 20;
	int* ptr;
	printf("\n i의 값 = %d \n j의 값 = %d", i, j);
	printf("\n i의 메모리 주소(&i) = %u", &i);
	printf("\n j의 메모리 주소(&j) = %u", &j);

	ptr = &i;
	printf("\n\n << ptr=&i 실행 >>");
	printf("\n ptr의 메모리 주소(&ptr) = %u", &ptr);
	printf("\n ptr의 값(ptr) = %u", ptr);
	printf("\n ptr의 참조 값 (*ptr)= %u", *ptr);

	ptr = &j;
	printf("\n\n << ptr=&j 실행 >>");
	printf("\n ptr의 메모리 주소(&ptr) = %u", &ptr);
	printf("\n ptr의 값(ptr) = %u", ptr);
	printf("\n ptr의 참조 값 (*ptr)= %u", *ptr);

	i = *ptr;
	printf("\n\n << i=*ptr 실행 >>");
	printf("\n i의 값 = %d", i);
}
```

---

### Ex_3_2
```c
#include <stdio.h>

void main() {
	char str[6] = "Korea";
	int i;

	for(i=0;i<6;i++){
		printf("%c의 메모리 주소 = %p\n", str[i], &str[i]);
	}

	char* ptr;
	ptr = &str[0];


	printf("ptr = %p\n", ptr);
	printf("str의 시작 주소 값은 %p\n", &str);
	printf("ptr의 참조 값 = %c\n", *ptr);

	for(i=0;i<5;i++){
		printf("%d: ptr의 참조 값은 %c\n", i, *ptr);
		ptr = ptr + 1;

	}
}
```

---

---

### Ex_3_3
```c
#include <stdio.h>

int main() {
	char str[] = "This is C programming";
	char* ptr;
	int i;

	ptr = str;
	while(*ptr)
		printf("%c", *ptr++);

	printf("\n");

	ptr = str;
	for(i=0; i<strlen(str);i++)
		printf("%c", *(ptr + i));

	printf("\n");

	return 0;
}
```

---

---

### Ex_3_4
```c
#include <stdio.h>

int main() {
	int i;
	char string1[20]="Dreams come true!", string2[20], *ptr1, *ptr2;

	ptr1 = string1;
	printf("\n string1의 주소 = %u \t ptr1 = %u", string1, ptr1);
	printf("\n string1 = %s \n ptr1 = %s", string1, ptr1);
	printf("\n\n (1)%s", ptr1 + 7);
	ptr2 = &string1[7];
	printf("\n (2)%s \n\n", ptr2);

	for (i = 16; i >= 0; i--) {
		putchar(*(ptr1 + i));
	}

	for (i = 0; i < 20; i++) {
		string2[i] = *(ptr1 + i);
	}

	printf("\n\n string1 = %s", string1);
	printf("\n string2 = %s \n", string2);

	*ptr1 = 'P';
	*(ptr1 + 1) = 'e';
	*(ptr1 + 2) = 'a';
	*(ptr1 + 3) = 'c';
	*(ptr1 + 4) = 'e';

	printf("\n string1 = %s \n", string1);
	return 0;
}
```

---

### Ex_3_5
```c

```
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<malloc.h>

int main() {
	
	int i, length;
	float* fnum;

	//길이를 지정하는 값을 입력받음

	printf("input a integer (5~10) : ");
	scanf("%d", &length);

	fnum = (float*)malloc(sizeof(float) * length);

	for (i = 0; i < length; i++) {
		*(fnum + i) = i;

	}
	printf("\n using fnum[i]: ");

	for(i=0;i<length;i++)
		printf("\n %.2f", fnum[i]);

	free(fnum); //메모리 해제



	return 0;
}
---

### Ex_3_6
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<malloc.h>

struct complex {
	float real;
	float imag;
};

int main() {
	
	int i, length;
	struct complex* cnum;

	//길이를 지정하는 값을 입력받음

	printf("input a integer (5~10) : ");
	scanf("%d", &length);

	cnum = (struct complex*)malloc(sizeof(struct complex) * length);

	//구조체 배열의 각 요소에 접근하여 값 설정
	for (i = 0; i < length; i++) {
		cnum[i].real = i;
		cnum[i].imag = i;
	}
	
	printf("\n using cnum[i].real, cnum[i].imag : \n");
	
	for(i=0;i<length;i++)
		printf("\n cnum[%d] = %.2f + %.2f * i", i, cnum[i].real, cnum[i].imag);

	printf("\n =======================================");

	printf("\n using (cnum+i) -> real, (cnum+i) -> imag : \n");

	for (i = 0; i < length; i++)
		//printf("\n cnum[%d] = %.2f + %.2f * i", i, cnum->real, cnum->imag);

	free(cnum); //메모리 해제



	return 0;
}
```

---

### Ex_3_7
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<malloc.h>


void printInteger(int number); // int 형 숫자 number를 인수로 받아 화면에 출력
void printPinteger(int *pi); // int 형 숫자 number를 인수로 받아 화면에 출력


int main() {

	int i = 10;

	printInteger(i); // int 형 숫자 i를 인수로 전달
	printPinteger(&i); // int 형 숫자 i의 주소를 인수로 전달
	
	return 0;
}

void printInteger(int number){
	printf("printInteger() : number = %d\n", number);
}

void printPinteger(int* pi){
	printf("printPinteger() : number = %d\n", *pi);
}
```

---

### Ex_3_8
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<malloc.h>

struct complex {
	double real;
	double imag;
};

void printInteger(int number); // int 형 숫자 number를 인수로 받아 화면에 출력
void printPinteger(int *pi); // int 형 숫자 number를 인수로 받아 화면에 출력
void printComplex(struct complex number);
void printPcomplex(struct complex* pc);

int main() {

	struct complex x = { 1,2 };
	int i = 10;

	printComplex(x); // int 형 숫자 i를 인수로 전달
	printPcomplex(&x); // int 형 숫자 i의 주소를 인수로 전달
	
	return 0;
}

void printComplex(struct complex number){
printf("printComplex() : x = %.2f + %.2f * i\n", number.real, number.imag);
}

void printPcomplex(struct complex* pc){
	printf("printComplex() : x = %.2f + %.2f * i\n", pc->real, pc->imag);
}
```

---

### Ex_3_9
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include<malloc.h>

void compute(float x, float y, float* sum, float* diff, float* average);

int main() {

	int i = 10;
	float mx=0, my=0, msum, mdiff, maverage;
	msum = mx + my;
	mdiff = mx - my;
	maverage = msum / 2;

	printf("Enter two numbers: ");
	scanf("%f %f", &mx, &my);

	compute(mx,my, &msum, &mdiff, &maverage); // int 형 숫자 i를 인수로 전달
	printf("sum = %f, diff = %f, average = %f\n", msum, mdiff, maverage);

	return 0;
}

void compute(float x, float y, float* sum, float* diff, float* average) {
	*sum = x + y;
	*diff = x - y;
	*average = *sum / 2;
}

```

---

### Ex_3_10
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void square(int x);
void cube(int x);

int main() {
	
	void(*myfunc)(int);
	int num;

	printf("Input a number : ");
	scanf("%d", &num);

	myfunc = square;
	(*myfunc)(num);

	myfunc = cube;
	(*myfunc)(num);


	return 0;
}

void square(int x) {
	printf("Square of %d is %d\n", x, x * x);
}

void cube(int x) {
	printf("Cube of %d is %d\n", x, x * x * x);
}

```

---

### Ex_3_11
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void square(int x);
void cube(int x);
void show(void (*pt)(int), int x);

int main() {
	
	void(*myfunc)(int);
	int num, work;

	printf("\n 1: suare \n 2 : cube \n\n 할 일을 선택하세요 : ");
	scanf("%d", &work);

	printf("\n Input a number x : ");
	scanf("%d", &num);

	if (work == 1)
		myfunc = square;
	else
		myfunc = cube;

	show(myfunc, num);

	return 0;
}

void square(int x) {
	printf("Square of %d is %d\n", x, x * x);
}

void cube(int x) {
	printf("Cube of %d is %d\n", x, x * x * x);
}

void show(void (*pt)(int), int x) {
	(*pt)(x);
}
```

---

### Ex_3_12  #오류 뜸
```c
#define _CRT_SECURE_NO_WARNINGS
#define SQUARE(a)
#define MIN(a,b)
#include <stdio.h>


int main() {
	
	int i = 2, j = 3, inum;

	float x = 1.5, y = 2.5, fnuml;

	printf("square(i+j = %d \n");
	printf("MIN(i,j) = %d", MIN(i, j));

	return 0;
}

void square(int x) {
	printf("Square of %d is %d\n", x, x * x);
}

void cube(int x) {
	printf("Cube of %d is %d\n", x, x * x * x);
}

void show(void (*pt)(int), int x) {
	(*pt)(x);
}
```

---

### Ex_3_
```c

```

---

### Ex_3_
```c

```
